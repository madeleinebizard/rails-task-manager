<h1>Index</h1>
<ul>
  <h1>My tasks</h1>
  <% @tasks.each do |task| %>

    <li> <%= task.completed %> - <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Add a new task", new_task_path %>
